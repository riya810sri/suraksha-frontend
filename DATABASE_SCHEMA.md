# 🗄️ Database Schema - Suraksha

Complete Firestore database structure and relationships.

---

## Database Overview

**Project:** womern-safety  
**Database Type:** Firestore NoSQL  
**Region:** Auto-selected by Firebase  
**Mode:** Production

---

## Collection Structure

```
Firestore Database
│
├── users/                          # User profiles
│   └── {userId}/
│       ├── name: string
│       ├── email: string
│       ├── phone: string
│       ├── safetyScore: number
│       ├── photoURL: string
│       ├── createdAt: timestamp
│       ├── updatedAt: timestamp
│       │
│       ├── contacts/               # Subcollection
│       │   └── {contactId}/
│       │       ├── name: string
│       │       ├── relation: string
│       │       ├── phone: string
│       │       ├── email: string
│       │       └── createdAt: timestamp
│       │
│       ├── incidents/              # Subcollection
│       │   └── {incidentId}/
│       │       ├── type: string ('sos' | 'location' | 'checkin' | 'alert')
│       │       ├── message: string
│       │       ├── location: GeoPoint
│       │       ├── status: string
│       │       ├── contactsNotified: number
│       │       └── createdAt: timestamp
│       │
│       └── locations/              # Subcollection
│           └── {locationId}/
│               ├── latitude: number
│               ├── longitude: number
│               ├── sharedWith: array
│               └── createdAt: timestamp
│
├── community/                      # Public posts
│   └── {postId}/
│       ├── title: string
│       ├── content: string
│       ├── category: string
│       ├── authorId: string
│       ├── likes: number
│       ├── comments: number
│       ├── createdAt: timestamp
│       └── updatedAt: timestamp
│
├── volunteers/                     # Volunteer profiles
│   └── {volunteerId}/
│       ├── name: string
│       ├── specialty: string
│       ├── rating: number
│       ├── helped: number
│       ├── location: string
│       ├── available: boolean
│       ├── photoURL: string
│       └── joinedAt: timestamp
│
└── publicIncidents/                # Public safety incidents
    └── {incidentId}/
        ├── location: string
        ├── type: string
        ├── status: string
        ├── reportedBy: string
        ├── createdAt: timestamp
        └── resolvedAt: timestamp
```

---

## Collection Details

### 1. users (Collection)

**Purpose:** Store user profile information.

**Document ID:** Firebase Authentication UID

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `name` | string | ✅ | - | User's full name |
| `email` | string | ✅ | - | Email address |
| `phone` | string | ✅ | - | Phone number |
| `safetyScore` | number | ✅ | 100 | Safety score (0-100) |
| `photoURL` | string | ❌ | null | Profile photo URL |
| `createdAt` | timestamp | ✅ | serverTimestamp | Account creation time |
| `updatedAt` | timestamp | ✅ | serverTimestamp | Last update time |

**Example Document:**
```json
{
  "name": "Priya Sharma",
  "email": "priya@example.com",
  "phone": "+91 9876543210",
  "safetyScore": 95,
  "photoURL": "https://firebasestorage.googleapis.com/...",
  "createdAt": "2026-02-28T10:30:00Z",
  "updatedAt": "2026-02-28T12:45:00Z"
}
```

**Subcollections:**
- `contacts` - Emergency contacts
- `incidents` - SOS alerts and incidents
- `locations` - Location sharing history

**Indexes:**
- None required (accessed by document ID only)

---

### 2. users/{userId}/contacts (Subcollection)

**Purpose:** Store user's emergency contacts.

**Document ID:** Auto-generated by Firestore

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `name` | string | ✅ | - | Contact's full name |
| `relation` | string | ✅ | - | Relationship (e.g., "Father") |
| `phone` | string | ✅ | - | Phone number |
| `email` | string | ❌ | null | Email address |
| `createdAt` | timestamp | ✅ | serverTimestamp | Added date |

**Example Document:**
```json
{
  "name": "Rajesh Kumar",
  "relation": "Father",
  "phone": "+91 9876543210",
  "email": "rajesh@example.com",
  "createdAt": "2026-02-28T10:30:00Z"
}
```

**Queries:**
```javascript
// Get all contacts for a user
const contacts = await getEmergencyContacts(userId);
// ORDER BY createdAt DESC
```

---

### 3. users/{userId}/incidents (Subcollection)

**Purpose:** Store SOS alerts and safety incidents.

**Document ID:** Auto-generated by Firestore

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `type` | string | ✅ | - | Event type |
| `message` | string | ❌ | null | Alert message |
| `location` | GeoPoint | ❌ | null | GPS coordinates |
| `status` | string | ✅ | 'active' | Event status |
| `contactsNotified` | number | ❌ | 0 | Contacts notified count |
| `createdAt` | timestamp | ✅ | serverTimestamp | Incident time |

**Type Values:**
- `sos` - SOS emergency alert
- `location` - Location share event
- `checkin` - Safe check-in
- `alert` - Safety alert

**Status Values:**
- `active` - Currently active
- `resolved` - Resolved
- `completed` - Completed

**Example Document:**
```json
{
  "type": "sos",
  "message": "Emergency! Need help immediately.",
  "location": {
    "latitude": 28.6139,
    "longitude": 77.2090
  },
  "status": "resolved",
  "contactsNotified": 3,
  "createdAt": "2026-02-28T14:30:00Z"
}
```

**Queries:**
```javascript
// Get all incidents for a user
const incidents = await getUserIncidents(userId);
// ORDER BY createdAt DESC

// Get active SOS alerts
const activeAlerts = await getActiveSOSAlerts(userId);
// WHERE type == 'sos' AND status == 'active'
```

---

### 4. users/{userId}/locations (Subcollection)

**Purpose:** Track location sharing history.

**Document ID:** Auto-generated by Firestore

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `latitude` | number | ✅ | - | Latitude coordinate |
| `longitude` | number | ✅ | - | Longitude coordinate |
| `sharedWith` | array | ❌ | [] | Contact IDs |
| `createdAt` | timestamp | ✅ | serverTimestamp | Share time |

**Example Document:**
```json
{
  "latitude": 28.6139,
  "longitude": 77.2090,
  "sharedWith": ["contact1", "contact2"],
  "createdAt": "2026-02-28T15:00:00Z"
}
```

---

### 5. community (Collection)

**Purpose:** Public discussion posts.

**Document ID:** Auto-generated by Firestore

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `title` | string | ✅ | - | Post title |
| `content` | string | ✅ | - | Post content |
| `category` | string | ✅ | - | Post category |
| `authorId` | string | ✅ | - | Author's user ID |
| `likes` | number | ✅ | 0 | Like count |
| `comments` | number | ✅ | 0 | Comment count |
| `createdAt` | timestamp | ✅ | serverTimestamp | Post time |
| `updatedAt` | timestamp | ✅ | serverTimestamp | Last update |

**Category Values:**
- `tips` - Safety tips
- `help` - Help & support
- `event` - Events
- `story` - Success stories
- `feedback` - Feedback

**Example Document:**
```json
{
  "title": "Best safety practices for late night travel",
  "content": "Always share your live location...",
  "category": "tips",
  "authorId": "user123",
  "likes": 128,
  "comments": 45,
  "createdAt": "2026-02-28T09:00:00Z",
  "updatedAt": "2026-02-28T16:30:00Z"
}
```

**Queries:**
```javascript
// Get all posts
const posts = await getCommunityPosts();
// ORDER BY createdAt DESC

// Get posts by category
const tips = await getPostsByCategory('tips');
// WHERE category == 'tips'
```

---

### 6. volunteers (Collection)

**Purpose:** Volunteer profiles.

**Document ID:** Auto-generated by Firestore

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `name` | string | ✅ | - | Volunteer name |
| `specialty` | string | ✅ | - | Area of expertise |
| `rating` | number | ✅ | 0 | Rating (0-5) |
| `helped` | number | ✅ | 0 | People helped count |
| `location` | string | ✅ | - | City/area |
| `available` | boolean | ✅ | true | Availability status |
| `photoURL` | string | ❌ | null | Profile photo |
| `joinedAt` | timestamp | ✅ | serverTimestamp | Join date |

**Example Document:**
```json
{
  "name": "Dr. Anjali Mehta",
  "specialty": "Medical Professional",
  "rating": 4.9,
  "helped": 234,
  "location": "Delhi",
  "available": true,
  "photoURL": "https://firebasestorage.googleapis.com/...",
  "joinedAt": "2026-01-15T08:00:00Z"
}
```

**Queries:**
```javascript
// Get available volunteers
const volunteers = await getAvailableVolunteers();
// WHERE available == true

// Get top rated volunteers
const topVolunteers = await getTopVolunteers();
// ORDER BY rating DESC LIMIT 10
```

---

### 7. publicIncidents (Collection)

**Purpose:** Public safety incident reports.

**Document ID:** Auto-generated by Firestore

**Fields:**

| Field | Type | Required | Default | Description |
|-------|------|----------|---------|-------------|
| `location` | string | ✅ | - | Location description |
| `type` | string | ✅ | - | Incident type |
| `status` | string | ✅ | 'active' | Status |
| `reportedBy` | string | ❌ | null | Reporter user ID |
| `createdAt` | timestamp | ✅ | serverTimestamp | Report time |
| `resolvedAt` | timestamp | ❌ | null | Resolution time |

**Type Values:**
- `emergency` - Emergency incident
- `alert` - Safety alert
- `hazard` - Environmental hazard

**Status Values:**
- `active` - Currently active
- `resolved` - Resolved

**Example Document:**
```json
{
  "location": "Near Metro Station, Dwarka",
  "type": "emergency",
  "status": "resolved",
  "reportedBy": "user123",
  "createdAt": "2026-02-28T13:00:00Z",
  "resolvedAt": "2026-02-28T14:30:00Z"
}
```

---

## Security Rules

### Firestore Rules (`firestore.rules`)

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Users collection
    match /users/{userId} {
      // User can only access their own data
      allow read, write: if request.auth != null 
                         && request.auth.uid == userId;
      
      // Contacts subcollection
      match /contacts/{contactId} {
        allow read, write: if request.auth != null 
                           && request.auth.uid == userId;
      }
      
      // Incidents subcollection
      match /incidents/{incidentId} {
        allow read, write: if request.auth != null 
                           && request.auth.uid == userId;
      }
      
      // Locations subcollection
      match /locations/{locationId} {
        allow read, write: if request.auth != null 
                           && request.auth.uid == userId;
      }
    }
    
    // Public incidents - anyone can read
    match /publicIncidents/{incidentId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Community posts - anyone can read
    match /community/{postId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Volunteers - anyone can read
    match /volunteers/{volunteerId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

---

## Storage Structure

### Firebase Storage Paths

```
storage/
├── profilePictures/
│   └── {userId}/
│       └── {filename}_{timestamp}.jpg
├── incidents/
│   └── {incidentId}/
│       └── {filename}.jpg
└── community/
    └── {postId}/
        └── {filename}.jpg
```

### Storage Rules (`storage.rules`)

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Profile pictures
    match /profilePictures/{userId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null 
                   && request.auth.uid == userId;
    }
    
    // Incident images
    match /incidents/{incidentId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
    // Community images
    match /community/{postId}/{fileName} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
```

---

## Data Relationships

### Entity Relationship Diagram

```
┌─────────────┐
│   users     │
│  (parent)   │
└──────┬──────┘
       │
       ├──────────────┐
       │              │
       ▼              ▼
┌─────────────┐ ┌─────────────┐
│  contacts   │ │  incidents  │
│ (subcollection)│ (subcollection)│
└─────────────┘ └─────────────┘
       │              │
       │              │
       ▼              ▼
┌─────────────┐ ┌─────────────┐
│  locations  │ │ community   │
│ (subcollection)│ (collection) │
└─────────────┘ └──────┬──────┘
                       │
                       │ authorId
                       ▼
                ┌─────────────┐
                │   users     │
                │  (reference)│
                └─────────────┘

┌─────────────┐
│ volunteers  │
│ (collection)│
└─────────────┘

┌─────────────┐
│publicIncidents│
│ (collection)│
└─────────────┘
```

---

## Common Queries

### User Profile Queries

```javascript
// Get user profile
const profile = await getUserProfile(uid);

// Update user profile
await updateUserProfile(uid, { safetyScore: 95 });

// Upload profile photo
const photoURL = await uploadProfilePhoto(uid, file);
```

### Contact Queries

```javascript
// Add emergency contact
await addEmergencyContact(uid, {
  name: 'Jane Doe',
  relation: 'Sister',
  phone: '+91 9876543210'
});

// Get all contacts
const contacts = await getEmergencyContacts(uid);
```

### Incident Queries

```javascript
// Create SOS alert
await addSOSAlert(uid, {
  message: 'Emergency!',
  location: { latitude, longitude },
  status: 'active'
});

// Get incident history
const incidents = await getUserIncidents(uid);
// Returns sorted by createdAt DESC
```

### Community Queries

```javascript
// Create post
await createCommunityPost(uid, {
  title: 'Safety tips',
  content: '...',
  category: 'tips'
});

// Get all posts
const posts = await getCommunityPosts();

// Like post
await likePost(postId);
```

---

## Indexes

**Current Indexes:** None required (using simple queries)

**Future Indexes (if needed):**

```json
{
  "indexes": [
    {
      "collectionGroup": "incidents",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "type", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    },
    {
      "collectionGroup": "community",
      "queryScope": "COLLECTION",
      "fields": [
        { "fieldPath": "category", "order": "ASCENDING" },
        { "fieldPath": "createdAt", "order": "DESCENDING" }
      ]
    }
  ]
}
```

---

## Data Migration

### Backup Strategy

1. **Automated Backups:** Firebase automatic backups (daily)
2. **Manual Export:** Use Firebase CLI for manual exports
3. **Version Control:** Keep schema changes in version control

### Export Command

```bash
firebase firestore:export gs://womern-safety-backups/backup-2026-02-28
```

### Import Command

```bash
firebase firestore:import gs://womern-safety-backups/backup-2026-02-28
```

---

## Performance Considerations

### Best Practices

✅ Use subcollections for user-specific data  
✅ Limit query result size with pagination  
✅ Use indexes for complex queries  
✅ Cache frequently accessed data client-side  
✅ Batch writes when possible  

### Query Limits

- Maximum document size: 1 MB
- Maximum write batch size: 500 operations
- Maximum query depth: 30 levels
- Rate limits: 100,000 reads/day (free tier)

---

**End of Database Schema Documentation**
